export class Offre {
    id_offre ? : String;
    nom ? : String;
    categorie? : String;
    description? : String;
    fichier ? : FileList;
    mode_payement ? : String;
    budget ?: number;
    rating? : number;
    max_horaire ?: number; 
    min_horaire ?: number;
    

    

}
